# Локальная разработка (Frontend + Backend)

## 🎯 Быстрый старт

### 1. Запуск Backend (Go)

```bash
cd backend

# Установите зависимости
go mod download

# Создайте .env файл
cp .env.example .env
# Отредактируйте .env и добавьте V0_API_KEY

# Запустите сервер
go run main.go
```

Backend будет доступен на `http://localhost:8080`

### 2. Запуск Frontend

В другом терминале:

```bash
# Из корня проекта
cd /Users/antonsekoldin/Documents/CursorProjects/poligraf-

# Запустите простой HTTP сервер
python3 -m http.server 8000
```

Frontend будет доступен на `http://localhost:8000`

### 3. Обновите API_BASE в app.js

Временно измените в `app.js`:

```javascript
// Для локальной разработки
const API_BASE = 'http://localhost:8080';
const API_GENERATE = `${API_BASE}/api/generate`;
```

Или создайте файл `app.local.js` с локальными настройками.

## 🔄 Workflow

1. **Backend запущен** на `http://localhost:8080`
2. **Frontend запущен** на `http://localhost:8000`
3. **Frontend отправляет запросы** на `http://localhost:8080/api/generate`
4. **Backend проксирует** запросы к v0.dev API

## 🛠️ Разработка

### Hot Reload для Backend (опционально)

Установите `air`:
```bash
go install github.com/cosmtrek/air@latest
```

Запустите:
```bash
cd backend
air
```

### Hot Reload для Frontend

Можно использовать любой dev server с live reload, например:
- `python3 -m http.server` (простой, без hot reload)
- `npx serve` (с live reload)
- `npx http-server` (с live reload)

## 📝 Коммиты

Коммиты делаются как обычно:

```bash
git add .
git commit -m "Описание изменений"
git push
```

**Важно:** Не коммитьте `.env` файл с API ключами!

## 🚀 Деплой

Пока деплой не используется (чтобы не тратить деньги на Vercel).

В будущем можно задеплоить:
- Backend: Railway, Render, Fly.io (бесплатные тарифы)
- Frontend: GitHub Pages (бесплатно)

## 🔍 Проверка работы

1. Откройте `http://localhost:8000`
2. Введите промпт
3. Нажмите "Отправить"
4. Проверьте логи backend в терминале
5. Результат должен отобразиться на frontend

## ⚠️ Troubleshooting

### Backend не запускается

- Проверьте, что Go установлен: `go version`
- Проверьте, что порт 8080 свободен: `lsof -i :8080`
- Проверьте, что `.env` файл существует и содержит `V0_API_KEY`

### Frontend не может подключиться к Backend

- Проверьте CORS настройки в `backend/main.go`
- Проверьте, что `API_BASE` в `app.js` указывает на `http://localhost:8080`
- Проверьте логи backend на наличие ошибок

### API запросы не работают

- Проверьте, что `V0_API_KEY` правильный в `.env`
- Проверьте логи backend для деталей ошибок
- Проверьте, что v0.dev API доступен (может быть проблема с подпиской)

