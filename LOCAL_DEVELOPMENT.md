# Локальная разработка (Frontend + Backend)

## 🎯 Быстрый старт

### 1. Запуск Backend (Go)

```bash
cd backend

# Установите зависимости
go mod download

# Создайте .env файл
cp .env.example .env
# Отредактируйте .env и добавьте V0_API_KEY

# Запустите сервер
go run main.go
```

Backend будет доступен на `http://localhost:8080`

### 2. Готово! Frontend уже работает

Go сервер отдает и статику (frontend), и API. Просто откройте:

**http://localhost:8080**

### 3. Обновите API_BASE в app.js (если нужно)

Временно измените в `app.js`:

```javascript
// Для локальной разработки
const API_BASE = 'http://localhost:8080';
const API_GENERATE = `${API_BASE}/api/generate`;
```

Или создайте файл `app.local.js` с локальными настройками.

## 🔄 Workflow

1. **Go сервер запущен** на `http://localhost:8080`
2. **Отдает статику** (HTML, JS, CSS) напрямую
3. **API endpoints** доступны на `/api/*`
4. **Все работает из одного сервера!** Не нужен Python или другой HTTP сервер

## 🛠️ Разработка

### Hot Reload для Backend (опционально)

Установите `air`:
```bash
go install github.com/cosmtrek/air@latest
```

Запустите:
```bash
cd backend
air
```

### Hot Reload для Frontend

Go сервер отдает статику напрямую. Для hot reload можно:
- Использовать `air` для backend (автоперезагрузка при изменении Go кода)
- Обновлять страницу вручную при изменении frontend файлов
- Или использовать браузерные dev tools с live reload

## 📝 Коммиты

Коммиты делаются как обычно:

```bash
git add .
git commit -m "Описание изменений"
git push
```

**Важно:** Не коммитьте `.env` файл с API ключами!

## 🚀 Деплой

Пока деплой не используется (чтобы не тратить деньги на Vercel).

В будущем можно задеплоить:
- Backend: Railway, Render, Fly.io (бесплатные тарифы)
- Frontend: GitHub Pages (бесплатно)

## 🔍 Проверка работы

1. Откройте `http://localhost:8080` (один сервер для всего!)
2. Введите промпт
3. Нажмите "Отправить"
4. Проверьте логи в терминале где запущен `go run main.go`
5. Результат должен отобразиться на странице

## ⚠️ Troubleshooting

### Backend не запускается

- Проверьте, что Go установлен: `go version`
- Проверьте, что порт 8080 свободен: `lsof -i :8080`
- Проверьте, что `.env` файл существует и содержит `V0_API_KEY`

### Frontend не может подключиться к Backend

- Проверьте CORS настройки в `backend/main.go`
- Проверьте, что `API_BASE` в `app.js` указывает на `http://localhost:8080`
- Проверьте логи backend на наличие ошибок

### API запросы не работают

- Проверьте, что `V0_API_KEY` правильный в `.env`
- Проверьте логи backend для деталей ошибок
- Проверьте, что v0.dev API доступен (может быть проблема с подпиской)

