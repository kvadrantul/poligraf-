# 🚀 Запуск Stable Diffusion 3.5 Medium API

## Быстрый старт

### Вариант 1: Docker (рекомендуется, если есть GPU)

```bash
cd stable-diffusion-api

# Запуск с GPU
docker-compose up -d

# Проверка логов
docker-compose logs -f

# Проверка работы
curl http://localhost:7861/health
```

### Вариант 2: Локально (Python)

```bash
cd stable-diffusion-api

# Создаем виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Устанавливаем зависимости
pip install -r requirements.txt

# Запускаем сервер
python main.py
```

## ⚠️ Важные замечания

1. **Первая загрузка модели** займет 10-20 минут и ~20GB дискового пространства
2. **GPU рекомендуется** - на CPU генерация может занять 5-10 минут на изображение
3. **Модель кешируется** в `~/.cache/huggingface` после первой загрузки

## 🔍 Проверка работы

```bash
# Health check
curl http://localhost:7861/health

# Тестовая генерация (может занять 30-60 секунд)
curl -X POST http://localhost:7861/generate \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "красивая визитка с золотыми элементами",
    "width": 1024,
    "height": 1024
  }'
```

## 📝 Настройка в Go backend

Go backend автоматически использует `http://localhost:7861` по умолчанию.

Для изменения URL установите переменную окружения:
```bash
export STABLE_DIFFUSION_API_URL=http://localhost:7861
```

## 🐛 Troubleshooting

**Проблема**: Модель не загружается
- Проверьте доступное место на диске (нужно ~20GB)
- Проверьте интернет-соединение (модель скачивается с Hugging Face)

**Проблема**: Ошибка CUDA/GPU
- Убедитесь, что установлены драйверы NVIDIA
- Для CPU режима убедитесь, что `torch` установлен без CUDA

**Проблема**: Медленная генерация
- Используйте GPU для ускорения
- Уменьшите `num_inference_steps` (но качество может снизиться)

