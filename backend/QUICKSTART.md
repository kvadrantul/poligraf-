# Быстрый старт Go Backend

## 🚀 Запуск

### 1. Установка зависимостей

```bash
cd backend
go mod download
```

### 2. Настройка .env

```bash
cp .env.example .env
# Отредактируйте .env и добавьте V0_API_KEY
```

### 3. Запуск

```bash
go run main.go
```

Сервер запустится на `http://localhost:8080`

**Важно:** Go сервер отдает и статику (frontend), и API! Не нужен отдельный HTTP сервер для frontend.

## 🔗 Интеграция с Frontend

### Обновите API_BASE в app.js

Временно измените в `app.js`:
```javascript
const API_BASE = 'http://localhost:8080';
```

Или используйте `app.local.js` в `index.html`:
```html
<script src="app.local.js"></script>
```

## ✅ Проверка

1. Go сервер запущен на `http://localhost:8080`
2. Откройте `http://localhost:8080` в браузере (frontend уже там!)
3. Отправьте тестовый запрос
4. Проверьте логи в терминале

## 📝 Логи

Логи backend видны в терминале, где запущен `go run main.go`
