# Настройка переменных окружения

## 🔑 Где взять API ключ?

### Вариант 1: Из Vercel (если уже задеплоено)

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект `poligraf-black`
3. Перейдите в **Settings** → **Environment Variables**
4. Найдите `V0_API_KEY`
5. Скопируйте значение

### Вариант 2: Из v0.dev напрямую

1. Откройте [v0.dev](https://v0.dev)
2. Перейдите в настройки: `https://v0.app/chat/settings/keys`
3. Создайте новый API ключ или скопируйте существующий

## 📝 Создание .env файла

```bash
cd backend
cp .env.example .env
```

Затем откройте `.env` и замените `your_v0_api_key_here` на реальный ключ:

```bash
V0_API_KEY=v1:ваш_реальный_ключ_здесь
```

## ✅ Проверка

После создания `.env` перезапустите сервер:

```bash
# Остановите текущий (Ctrl+C)
# Запустите заново
go run main.go
```

Сервер автоматически загрузит переменные из `.env`.

## ⚠️ Важно

- **НЕ коммитьте** `.env` файл в git (он уже в `.gitignore`)
- Храните ключ в безопасности
- Для production используйте переменные окружения на хостинге

